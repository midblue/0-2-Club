{
  "name": "0-2club",
  "version": "1.0.0",
  "description": "An esports fitbit for anyone getting started in competitive gaming.",
  "author": "Jasper Stephenson <jasperstephenson@gmail.com>",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch .",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "logs": "heroku logs -a zero-two-club --tail -s app",
    "l": "npm run logs || npm run logs || npm run logs || npm run logs || npm run logs || npm run logs || npm run logs || npm run logs || npm run logs || npm run logs || npm run logs || npm run logs || npm run logs",
    "rs": "heroku ps:restart -a zero-two-club && npm run l"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "chart.js": "^2.9.4",
    "connect-timeout": "^1.9.0",
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "firebase-admin": "^8.9.2",
    "nuxt": "^2.13.3",
    "socket.io": "^2.3.0",
    "source-map-support": "^0.5.16",
    "vue-chartjs": "^3.5.0",
    "vue-clickaway2": "^2.3.1",
    "vue-search-select": "^2.9.3",
    "vue-social-sharing": "^2.4.7"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "node-sass": "^4.14.1",
    "sass-loader": "^8.0.2",
    "smashgg.js": "^4.1.10"
  }
}
